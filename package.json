{
  "name": "collaborative-drawing-canvas",
  "version": "2.0.0",
  "type": "commonjs",
  "description": "Production-ready, low-latency collaborative whiteboarding platform with stroke-level event sourcing, per-user undo/redo, and multi-room isolation",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.14.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}